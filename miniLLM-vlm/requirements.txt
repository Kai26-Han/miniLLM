# PyTorch >=2.3,<3.0 is managed separately for the AutoDL GPU image.
# Verify the installed CUDA build first; do not select a new torch wheel here.
# Supported Transformers range for this standalone VLM implementation.
transformers>=4.51,<4.56
tokenizers>=0.20,<1.0
datasets>=3.0,<5.0
pyarrow>=17
Pillow>=10.0
numpy>=1.26
jinja2>=3.1,<4
safetensors>=0.4
# Optional; select --tracker swanlab to enable online/offline experiment logging.
swanlab>=0.9.5,<1.0
